
- Install a C++ compiler with C++11 support (tested with gcc 4.8.2).

- Install libsndfile (tested with 1.0.25).

- Build:

mkdir build
cd build
cmake -D CMAKE_BUILD_TYPE=Release ..
make

- Test:

../scripts/test.sh 'Hello world.'
